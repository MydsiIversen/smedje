# Umami database password — generate with: smedje password --length 32 --charset alphanum
UMAMI_DB_PASSWORD=

# Umami application secret — generate with: smedje password --length 64 --charset alphanum
UMAMI_APP_SECRET=

# Umami website ID — fill in after creating a website in the Umami dashboard
UMAMI_ID=
